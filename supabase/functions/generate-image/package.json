{
  "name": "generate-image",
  "version": "1.0.0",
  "description": "Edge function to generate images using OpenAI's DALL-E 3",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  }
} 